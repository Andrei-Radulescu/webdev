(function() {
    var fw = fw || {
        Array: function(arr) {

        },
        Map: function(keys, values) {
            if (keys && typeof keys === 'object' && !values) {
                ;
            }

        }
    };

    return fw;

});